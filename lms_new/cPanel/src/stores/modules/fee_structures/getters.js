export default {
    fee_structures(state) {
        return state.fee_structures;
    },

    fee_structure(state) {
        return state.fee_structure;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
