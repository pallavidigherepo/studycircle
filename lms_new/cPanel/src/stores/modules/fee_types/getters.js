export default {
    fee_types(state) {
        return state.fee_types;
    },

    fee_type(state) {
        return state.fee_type;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
