export default {
    clients(state) {
        return state.clients;
    },

    client(state) {
        return state.client;
    },

    datatable(state) {
        return state.datatable;
    },
    meta(state) {
        return state.pagination;
    },
}
