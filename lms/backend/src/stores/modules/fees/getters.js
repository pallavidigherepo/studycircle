export default {
    fees(state) {
        return state.fees;
    },

    fee(state) {
        return state.fee;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
