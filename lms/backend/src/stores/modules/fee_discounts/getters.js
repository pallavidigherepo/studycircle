export default {
    fee_discounts(state) {
        return state.fee_discounts;
    },

    fee_discount(state) {
        return state.fee_discount;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
