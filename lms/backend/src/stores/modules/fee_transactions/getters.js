export default {
    fee_transactions(state) {
        return state.fee_transactions;
    },

    fee_transaction(state) {
        return state.fee_transaction;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
