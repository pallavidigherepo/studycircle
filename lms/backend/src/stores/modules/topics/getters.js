export default {
    topics(state) {
        return state.topics;
    },
    
    datatable(state) {
        return state.datatable;
    },
    
    meta(state) {
        return state.pagination;
    }
}