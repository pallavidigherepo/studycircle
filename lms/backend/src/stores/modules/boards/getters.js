export default {
    boards(state) {
        return state.boards;
    },

    board(state) {
        return state.board;
    },

    boardList(state) {
        return state.boardList;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
