export default {
    batches(state) {
        return state.batches;
    },

    batch(state) {
        return state.batch;
    },

    batchList(state) {
        return state.batchList;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
