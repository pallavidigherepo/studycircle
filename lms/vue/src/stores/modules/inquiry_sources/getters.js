export default {
    inquiry_sources(state) {
        return state.inquiry_sources;
    },

    inquiry_source(state) {
        return state.inquiry_source;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
